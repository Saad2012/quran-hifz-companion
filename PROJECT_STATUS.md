# Project Status

## آخر تحديث

- 2026-05-11 13:20 تقريبًا بتوقيت الرياض

## ملخص المشروع

- تطبيق ويب عربي RTL لإدارة حفظ القرآن ومراجعته بأسلوب `local-first`.
- المشروع أصبح يدعم تسجيل الدخول والمزامنة السحابية عبر Supabase، مع بقاء التخزين المحلي فعالًا.

## ما تم إنجازه

- بناء صفحات التطبيق الأساسية: الرئيسية، الجلسات، المراجعة، المقاطع، الاختبارات، التجويد، التحليلات، التخطيط، الصفحات الضعيفة، التقارير، الإعدادات، والدليل.
- فصل منطق المجال عن الواجهة، بما في ذلك review engine وanalytics والتخزين.
- إضافة بيانات seed وتجربة RTL عربية.
- إضافة Supabase Auth وتدفق تسجيل دخول بالبريد الإلكتروني وكلمة المرور.
- إضافة تسجيل الدخول عبر Google فوق نفس callback الحالي.
- إضافة استعادة كلمة المرور وتعيين كلمة مرور جديدة.
- إضافة callback route و`proxy.ts` لتحديث جلسة Supabase مع Next.js 16.
- إضافة `auth/confirm` لتدفق recovery والتحقق من `token_hash`.
- إضافة طبقة مزامنة سحابية للبيانات باستخدام جدول `user_hifz_snapshots`.
- إضافة migration موثقة في `supabase/migrations/20260510_auth_and_sync.sql`.
- تحديث `.env.example` و`README.md` ليعكسا متطلبات البيئة الجديدة.
- إعداد مشروع Supabase فعليًا وإنشاء جدول `user_hifz_snapshots` بسياسات RLS.
- نشر نسخة production ناجحة على Vercel.
- توصيل مشروع Vercel بمستودع GitHub وتفعيل Git Integration.
- إضافة متغيرات البيئة نفسها إلى `Production` و`Development` و`Preview` على Vercel.
- إضافة صفحة `وضع التركيز` كمركز تنفيذ يومي للجلسة.
- إضافة مؤقت جلسة محلي مع حفظ تلقائي لحالة الجلسة على الجهاز.
- إضافة عداد تكرارات سريع مناسب للهاتف داخل وضع التركيز.
- إضافة `خطة التعافي` تلقائيًا عند الانقطاع لعدة أيام.
- تحسين الملاحة وتجربة الهاتف مع شريط سفلي مختصر ونافذة `المزيد`.

## الملفات التي تم تعديلها

- `features/app-state/provider.tsx`
  - أضيفت حالة المزامنة، والتحميل المحلي أولًا، والمصالحة مع السحابة ثم الرفع التلقائي.
  - السبب: جعل التطبيق local-first مع sync فعلي بدل استبدال التخزين المحلي بالكامل.
- `features/auth/provider.tsx`
  - أضيف موفّر جلسة Supabase على الواجهة.
  - السبب: مشاركة حالة المستخدم وتسجيل الخروج وتحديث الجلسة عبر التطبيق.
- `features/auth/auth-page.tsx`
  - أضيفت واجهة دخول وإنشاء حساب عربية مع زر Google OAuth.
  - السبب: تمكين الحسابات والمزامنة من داخل المنتج مع خيار دخول أسرع.
- `components/icons/google-icon.tsx`
  - أضيفت أيقونة Google قابلة لإعادة الاستخدام.
  - السبب: جعل زر Google واضحًا واحترافيًا داخل الواجهة.
- `app/login/page.tsx`
  - أضيفت صفحة دخول عامة خارج shell التطبيق.
  - السبب: فصل المسارات العامة عن المسارات المحمية.
- `app/auth/callback/route.ts`
  - أضيف route لتبادل كود التفعيل/الدخول مع Supabase.
  - السبب: دعم PKCE وتأكيد البريد وفق النمط الحالي لـ Supabase SSR.
- `app/auth/confirm/route.ts`
  - أضيف route للتحقق من `token_hash` وتوجيه recovery إلى شاشة إعادة التعيين.
  - السبب: دعم تدفق استعادة كلمة المرور الموصى به مع Supabase.
- `app/forgot-password/page.tsx` و`app/reset-password/page.tsx`
  - أضيفتا كصفحتين عامتين للاستعادة وتعيين كلمة مرور جديدة.
  - السبب: إكمال تجربة الحساب بدل الاكتفاء بالدخول فقط.
- `proxy.ts`
  - أضيف Next.js Proxy لتحديث كوكيز الجلسة.
  - السبب: إبقاء Server Components مطلعة على الجلسة الحالية.
- `lib/persistence/supabase-storage.ts`
  - أضيف adapter للمزامنة السحابية.
  - السبب: إبقاء persistence layer مفصولة بوضوح.
- `lib/supabase/*`
  - أضيفت helpers للبيئة، عميل المتصفح، عميل الخادم، وProxy helper.
  - السبب: تنظيم تكامل Supabase في طبقة مستقلة.
- `components/layout/app-shell.tsx`
  - أضيفت صفحة `وضع التركيز` إلى الملاحة، مع شريط سفلي أفضل للهاتف ونافذة `المزيد`.
  - السبب: جعل الوصول اليومي أسرع وأوضح على الشاشات الصغيرة.
- `features/focus/focus-page.tsx`
  - أضيفت شاشة تنفيذ يومية تربط الاقتراحات، خطة التعافي، والحفظ المباشر.
  - السبب: نقل التطبيق من أداة إدارة فقط إلى أداة تنفيذ عملية أثناء الجلسة.
- `features/focus/focus-session-console.tsx`
  - أضيفت لوحة مؤقت + عداد تكرارات + حفظ فوري للجلسة.
  - السبب: تقليل الاحتكاك أثناء الاستخدام الفعلي، خصوصًا على الهاتف.
- `hooks/use-focus-session.ts`
  - أضيف hook محلي لحفظ حالة جلسة التركيز واستئنافها.
  - السبب: عدم ضياع المؤقت أو التكرارات عند التنقل أو تحديث الصفحة.
- `lib/domain/recovery-plan.ts`
  - أضيف منطق اشتقاق خطة تعافٍ تلقائية بعد الانقطاع.
  - السبب: إعادة المستخدم للمسار بشكل منظم بدل العودة العشوائية.
- `lib/domain/app-view.ts` و`lib/domain/next-action.ts`
  - أضيف ربط التعافي ووضع التركيز داخل البيانات المشتقة والاقتراحات الذكية.
  - السبب: جعل الميزات الجديدة جزءًا من منطق المشروع نفسه لا مجرد واجهة منفصلة.
- `features/settings/settings-page.tsx`
  - أضيف قسم للحساب والمزامنة اليدوية.
  - السبب: منح المستخدم نقطة تحكم واضحة بالحساب والنسخ السحابي.
- `features/guide/content.ts`
  - أضيف شرح لوضع التركيز والمؤقت وخطة التعافي.
  - السبب: إبقاء الدليل مواكبًا للميزات الجديدة.

## الأوامر التي تم تشغيلها

- `npm run lint`
  - النتيجة: نجح.
- `npm run build`
  - النتيجة: نجح.
- `npm install @supabase/ssr @supabase/supabase-js`
  - النتيجة: نجح.
- تطبيق migration على Supabase
  - النتيجة: نجح.
- فحص Security Advisors في Supabase
  - النتيجة: ظهر تحذير `search_path` ثم تم إصلاحه وأصبح الفحص نظيفًا.
- `npx vercel link --yes --project quran-hifz-companion`
  - النتيجة: نجح الربط المحلي بالمشروع على Vercel.
- `npx vercel --prod --yes`
  - النتيجة: نجح النشر الإنتاجي.
- `npm run lint`
  - النتيجة: نجح بعد إضافة تدفق استعادة كلمة المرور.
- `npm run build`
  - النتيجة: نجح بعد إضافة الصفحات والمسارات الجديدة.
- `npm run lint`
  - النتيجة: نجح بعد إضافة Google login.
- `npm run build`
  - النتيجة: نجح بعد إضافة Google login.
- `npm run lint`
  - النتيجة: نجح بعد إضافة وضع التركيز وخطة التعافي وتحسين الهاتف.
- `npm run build`
  - النتيجة: نجح بعد إضافة وضع التركيز وخطة التعافي وتحسين الهاتف.
- توصيل GitHub من لوحة Vercel
  - النتيجة: أصبح المستودع `Saad2012/quran-hifz-companion` متصلًا بالمشروع.
- إضافة متغيرات بيئة `Preview` عبر Vercel API
  - النتيجة: أصبحت البيئات الثلاث `Development` و`Preview` و`Production` مضبوطة.

## المشاكل الحالية

- لا توجد اختبارات آلية مستقلة غير `lint` و`build`.
- ما يزال التخزين السحابي الحالي يعتمد على لقطة JSON واحدة لكل مستخدم.
- لا توجد بعد إدارة جلسات متقدمة للمستخدم أو تحديث البريد من داخل التطبيق.
- لا توجد بعد إشعارات push أو PWA لتكملة تجربة الهاتف.

## الخطوة التالية المقترحة

- إضافة `PWA + إشعارات` ثم توسيع وضع التركيز إلى `اختبار شفهي سريع` و`إيقاف/استئناف أكثر عمقًا`.

## ملاحظات أمان

- لا توجد أسرار حقيقية داخل الملفات المرفوعة للمستودع.
- `.env.local` يجب أن يبقى محليًا فقط، و`.env.example` آمن.
- `.gitignore` يستبعد ملفات البيئة والبناء والكاش، ويسمح فقط بـ `supabase/migrations/*.sql`.
- توجد ملفات كبيرة محلية مثل `.next/` و`node_modules/` ويجب ألا تُرفع.
- ملف `.env.local` موجود محليًا على الجهاز لتشغيل Supabase، لكنه مستبعد من Git.
- ملفات `Serena` المرفوعة مقتصرة على التهيئة والذكريات المفيدة، بينما `cache` و`logs` ما زالت مستبعدة.
